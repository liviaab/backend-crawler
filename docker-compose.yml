version: '3'
services:
  web:
    build: .
    ports:
    - "3333:3333"
  db:
    image: mysql:5.7
    ports:
      - "3306:3306"
    volumes:
      - ./data:/var/lib/mysql
    environment:
      - MYSQL_ROOT_PASSWORD=root
      - MYSQL_DATABASE=court_crawler
